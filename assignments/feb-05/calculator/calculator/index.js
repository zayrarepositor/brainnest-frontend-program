class Calculator {
  add(num1, num2) {
    return (num1 + num2).toFixed(2);
  }
  rest(num1, num2) {
    return (num1 - num2).toFixed(2);
  }
  mul(num1, num2) {
    return (num1 * num2).toFixed(2);
  }
  div(num1, num2) {
    return (num1 / num2).toFixed(2);
  }
}
